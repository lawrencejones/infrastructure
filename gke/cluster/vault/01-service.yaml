---
apiVersion: v1
kind: Service
metadata:
  name: vault
  annotations:
    external-dns.alpha.kubernetes.io/hostname: vault.lawrjone.xyz
spec:
  type: LoadBalancer
  ports:
    - name: http
      port: 433
      targetPort: 8200
    - name: server
      port: 8201
